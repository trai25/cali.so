import {
  amaConfirmationMetadata,
  AmaConfirmationPageView,
} from '../../../../../_views/ama-confirmation-page'

export const metadata = amaConfirmationMetadata('en')

export default function EnglishAmaConfirmationPage() {
  return <AmaConfirmationPageView />
}
